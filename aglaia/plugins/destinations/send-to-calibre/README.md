# send-to-calibre

See `aglaia-plugin.toml` for the manifest and the module docstring in the
entry file for what this destination does and why it does it that way.

Bundled with Aglaïa as a first-party destination, and kept in exactly the
layout the plugin registry uses — same manifest, same one-module rule — so
these three double as the worked example a submitted plugin is measured
against. See [docs/plugin-store.md](../../../../docs/plugin-store.md).
